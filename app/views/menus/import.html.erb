<h1>Importando Cardápios</h1>


<h5>Selecione o arquivo cardápio a ser importado</h5>
<%= form_for @menu_import do |f| %>
  <% if @menu_import.errors.any? %>
    <%= pluralize(@menu_import.errors.count, "erros") %> impediram esse cardápio de ser salvo.
    <ul>
      <% @menu_import.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  <% end %>

  <%= f.file_field :file %>
  <%= button_tag 'Importar Cardápio', name: 'route_to[create_sheet]' %>
<% end %>