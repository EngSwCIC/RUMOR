<%= form_with(model: review_lunch, url: [@lunch, @review_lunch]) do |form| %>
  <% if review_lunch.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(review_lunch.errors.count, "error") %> prohibited this review_lunch from being saved:</h2>

      <ul>
      <% review_lunch.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div id="star-rating"></div>

  <div class="form-group">
    <%= form.label :comment %>
    <%= form.text_area :comment, class: "form-control" %>
  </div>

  <div class="actions">
    <%= form.submit class: "btn btn-primary" %>
  </div>
<% end %>

<script>
  $('#star-rating').raty({
    path: '../../../assets',
    hints: ['terrível', 'ruim', 'normal', 'bom', 'excelente'],
    scoreName: 'review_lunch[rating]'
  });
</script>
